추가할 것
go lint 설정할 것

1. Config 파일 불러오기
2. res.Write 함수 공용 함수로 수정
3. DB 연결

4. Redis 연결
5. Panic >> HandleErr 로 처리 >> DB에 로그 남기기 처리


6. Redis로 Sequence 처리 추가(uid 처리?)

7. Log 추가
8. 에러 로그 trace 추가


9. Client(Unity) 연결


DB 설정 
1. read/write 구분 X 
2. DB 시간 서버 시간 >> utc 기준 시간으로 수정


DBQueryRow 호출 방식
    type rltType struct {
        Idx int64
    }
    func (r *rltType) MapToStruct(mapData map[string]interface{}) {
        r.Idx = mapData["idx"].(int64)
    }
    var rlt IDBModel = &rltType{}
    DBQueryRow(GameDB, "SELECT * FROM tbl_test;", rlt)
    fmt.Println(rlt)